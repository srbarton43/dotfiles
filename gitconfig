# This is Git's per-user configuration file.
[user]
# Please adapt and uncomment the following lines:
name = Samuel Barton
	email = srbarton43@gmail.com
	editor = nvim
[init]
	defaultBranch = main

[mergetool "nvim"]
  cmd = nvim -f -c \"Gvdiffsplit!\" \"$MERGED\"
 
[merge]
  tool = nvim

[mergetool]
  prompt = false
  keepBackup = false
